import streamlit as st
import pandas as pd

st.set_page_config(page_title="Clustering", layout="wide")

st.title("Behavioral Clustering")

st.markdown("""
Placeholder: methodology summary (Jaccard distance, average linkage,
gap-based cut heights, three-tier resolution) — condensed from the README.
""")

st.header("Cluster profiles")
st.write("Placeholder: cluster_main size breakdown + top activities per cluster.")

st.header("The grief-linked cluster")
st.write("Placeholder: objective, non-specific description of the 7-entry cluster and its stability across the data refresh.")
